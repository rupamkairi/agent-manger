# Bundled Fonts

Place the local desktop font files here:

- `Geist-Regular.woff2`
- `Geist-SemiBold.woff2`
- `JetBrainsMono-Regular.woff2`
- `JetBrainsMono-Medium.woff2`

The app never imports fonts from Google at runtime. CSS falls back to system sans/mono if these files are not present.
